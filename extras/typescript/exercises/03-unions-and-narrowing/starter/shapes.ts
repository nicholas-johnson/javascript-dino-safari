// TODO: define the Shape type (discriminated union with 'circle', 'rectangle', 'triangle').
// Then add type annotations and narrowing logic to each function.

export const area = (shape) => {
  // TODO: narrow by shape.kind and compute the area
};

export const describe = (shape) => {
  // TODO: narrow by shape.kind and return a description
};

export const formatValue = (value) => {
  // TODO: narrow string | number
};

export const getLength = (value) => {
  // TODO: narrow string | any[]
};

export const summarise = (item) => {
  // TODO: narrow string | number | boolean
};
