import { describe, it, expect } from 'vitest';
import { add, subtract, multiply, divide, clamp, isEven, factorial } from './math.js';

// TODO: write tests for every function exported from math.js.
// Use describe() to group related tests and it() for individual cases.
// Aim for at least two test cases per function — a typical case and an edge case.
