import { describe, it, expect } from 'vitest';
import {
  capitalize,
  slugify,
  truncate,
  countWords,
  reverse,
  isPalindrome,
  camelToKebab,
} from './strings.js';

// TODO: write tests for every function exported from strings.js.
// Think about edge cases: empty strings, single characters, strings at exact boundary lengths.
