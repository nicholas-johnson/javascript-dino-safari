import { describe, it, expect } from 'vitest';
import {
  safeDivide,
  parseJSON,
  requirePositive,
  lookupById,
  createUser,
} from './validators.js';

// TODO: write tests for every function exported from validators.js.
// Remember: expect(() => fn()).toThrow() needs a wrapper function.
// Test both the success path and the error path for each function.
