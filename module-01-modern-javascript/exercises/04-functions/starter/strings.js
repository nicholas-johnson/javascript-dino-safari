/**
 * Returns "Hello, <name>!".
 * @param {string} name
 */
export function greet(name) {
}

/**
 * Returns the first character of each name, uppercased, joined with dots.
 * e.g. initials("Ellie", "Sattler") → "E.S."
 * @param {string} first
 * @param {string} last
 */
export function initials(first, last) {
}
