/**
 * TODO: implement validateAlert (zone, level 1–5, timestamp > 0).
 * @param {{ zone?: unknown; level?: unknown; timestamp?: unknown }} alert
 * @returns {boolean}
 */
export function validateAlert(alert) {
  return false;
}
